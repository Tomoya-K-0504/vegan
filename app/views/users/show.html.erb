<%= render 'sessions/header' %>

<p id="notice"><%= notice %></p>

<section class="mypage">
  <h2>マイページ</h2>
  <%= image_tag 'rogo_full.png' %>
  <h3 class="name">
    <%= @user.first_name %>
    <%= @user.last_name %>
      <%= link_to '編集', edit_user_path(@user) %>
  </h3>
  <hr>
  <div>
    ひとことのカラム追加するひとことのカラム追加するひとことのカラム追加するひとことのカラム追加する
  </div>
  <hr>
  <div class="detail">
    <h3>詳細</h3>
    <p>
      <strong>性別 </strong>
      <%= @user.sex %>
      <strong>Age:</strong>
      <%= @user.age %>
      <strong>Job:</strong>
      <%= @user.job %>

      <strong>Born in:</strong>
      <%= @user.born_in %>

      <strong>Living address:</strong>
      <%= @user.living_address %>

<!--      <strong>Email:</strong>-->
      <%#= @user.email %>
  </div>
  <br>
  <div class="principle">
    <p>
      <strong>Principle:</strong>
      <%= @user.principle %>
    </p>

    <p>
      <strong>Trigger:</strong>
      <%= @user.trigger %>
    </p>
  </div>
  <br>
  <div class="contact">
    <p>
      <strong>Twitter:</strong>
      <%= @user.twitter %>
    </p>

    <p>
      <strong>Facebook:</strong>
      <%= @user.facebook %>
    </p>

    <p>
      <strong>Contact:</strong>
      <%= @user.contact %>
    </p>
  </div>
  <hr>
  <div>
    <p>
      <strong>Career:</strong>
      <%= @user.career %>
    </p>
  </div>

</section>
